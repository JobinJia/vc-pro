{
  "name": "@vc-view/docs",
  "version": "1.0.0",
  "scripts": {
    "dev:docs": "vite",
    "tsc:docs": "vue-tsc --noEmit",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue-router": "^4.0.12"
  }
}
